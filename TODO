O Trogl::Scene
	o add multi light control ( on, off, position, create , etc ... )

O Trogl::EventHandler    
	o add support for key repeats for key toggles binds

O Trogl::Object3d::Loader::WaveFrontObject
	o add support for changing the base reference
	o fix object scaling. works but scales the whole mvnt space, not just the object drawing

O Trogl::Object3d::Loader
	o add loader for other 3d object format. 3ds ?
